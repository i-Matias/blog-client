import * as React from "react";
import SignForm from "../../component/SignForm";
// import { useColorScheme } from "@mui/joy/styles";

const Register = () => {
  return (
    <main>
      <SignForm isLoggingIn={false} />
    </main>
  );
};

export default Register;
